package courses.abc.atoms.features.student;

import courses.abc.atoms.core.services.FeatureManagerService;
import courses.abc.atoms.core.util.LMSFeature;

public class StudentFeature extends LMSFeature {
    public StudentFeature(FeatureManagerService featureManagerService){
        super(featureManagerService);
    }
}
