package courses.abc.atoms.features.certificates;

import courses.abc.atoms.core.services.FeatureManagerService;
import courses.abc.atoms.core.util.LMSFeature;

public class Certificates extends LMSFeature{
    
    public Certificates(FeatureManagerService featureManagerService) {
        super(featureManagerService);
    }
}
