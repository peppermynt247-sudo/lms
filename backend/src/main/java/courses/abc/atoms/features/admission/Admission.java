package courses.abc.atoms.features.admission;

import courses.abc.atoms.core.services.FeatureManagerService;
import courses.abc.atoms.core.util.LMSFeature;

public class Admission extends LMSFeature{
    
    public Admission(FeatureManagerService featureManagerService) {
        super(featureManagerService);
    }
}
